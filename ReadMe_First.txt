First run the DatabaseCreation.js file to create database in mongodb with null entries.

Then run index.js to connect to database server.

Then in browser on localhost:3000 to run the website.

Note - pdf file is created with name as DATA_x.pdf where X is the roll number you entered in personal details form.
